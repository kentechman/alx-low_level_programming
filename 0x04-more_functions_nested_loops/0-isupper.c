#include "main.h"
/**
 * _isupper - the checked state
 * main - main entry
 * @c: character to be checked
 * Return: 1 or 0
 */
int _isupper(int alp)
{

if (alp >= 65 && alp <= 90)
{
return (1);
}
return (0);
}
