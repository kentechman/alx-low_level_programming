#include "main.h"
#include <string.h>
/**
 * _strlen - the lenght os the string
 * @s: the string lengt
 * Return: return string length
 */
int _strlen(char *s)
{
int length;
length = strlen(s);
return (length);
}
